import SwiftUI
import WidgetKit

/// Entry point for the widget extension. Keep `@main` only here.
@main
struct SurahPlannerWidgetBundle: WidgetBundle {
  var body: some Widget {
    SurahPlannerWidget()
  }
}
